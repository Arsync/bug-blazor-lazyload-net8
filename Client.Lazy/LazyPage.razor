@page "/lazy"

<h1>Lazy</h1>
<p>@DebugValue</p>

<a href="/">Home</a>

@code {
    public string DebugValue
    {
        get
        {
            Console.WriteLine(OperatingSystem.IsBrowser() ? "Running on browser" : "Not running on browser");
            System.Diagnostics.Debugger.Break();
            return "Debugger breakpoint hits in .NET 7 but not .NET 8";
        }
    }
}